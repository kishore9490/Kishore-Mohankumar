import type { ArchComponent, ArchLayer } from '@/types'

/**
 * The nine-layer architecture (§5) and the drawer detail for every component (§6).
 * This is the spine of the explorer — the Technical Architecture canvas, global
 * search, filters and zoom levels all read from here.
 */

const c = (x: ArchComponent): ArchComponent => x

export const COMPONENTS: ArchComponent[] = [
  // ─────────────────────────── LAYER 1 — EXPERIENCE ───────────────────────────
  c({
    id: 'enterprise-portal', name: 'Enterprise Portal', layer: 'experience',
    purpose: 'The requester-side workspace where an organization defines policy, runs campaigns and reviews verification outcomes.',
    responsibilities: ['Policy authoring and simulation', 'Campaign creation and tracking', 'Exception review and approval', 'Monitoring alerts and re-verification queues', 'Audit export'],
    inputs: ['Authenticated user session', 'Tenant context', 'Campaign and verification state'],
    outputs: ['Policy definitions', 'Campaign instructions', 'Approval decisions'],
    dependencies: ['Authentication', 'Tenant Context', 'Campaign Service', 'Policy Engine'],
    dataHandled: ['Tenant-scoped relationships', 'Verification outcomes', 'Assessment factors'],
    security: ['RBAC by function (procurement, compliance, finance)', 'Tenant isolation enforced server-side', 'No cross-tenant relationship visibility'],
    events: ['CampaignCreated', 'PolicyActivated', 'VerificationApproved'],
    apis: ['POST /v1/campaigns', 'POST /v1/policies', 'GET /v1/verification-requests'],
    related: ['campaign-service', 'policy-engine', 'customer-lifecycle'],
    facets: ['business', 'verification', 'lifecycle'], minZoom: 'executive', tone: 'primary',
  }),
  c({
    id: 'organization-portal', name: 'Organization Portal', layer: 'experience',
    purpose: 'The member-side workspace every organization gets for free — its own identity, credentials, consents and the requests made of it.',
    responsibilities: ['Claim and maintain the organization profile', 'Respond to verification invitations', 'Manage published credentials', 'Grant, review and revoke consents', 'See who holds access to what'],
    inputs: ['Invitation token or authenticated session', 'Pending verification requests'],
    outputs: ['Submitted attributes and documents', 'Consent decisions', 'Profile publication settings'],
    dependencies: ['Organization Service', 'Consent Service', 'Credential Service'],
    dataHandled: ['Own organization attributes', 'Own credentials', 'Own consent register'],
    security: ['Organization-scoped access only', 'Cannot see requester-side data of other tenants', 'Consent revocation always available'],
    events: ['InvitationAccepted', 'ConsentGranted', 'ConsentRevoked'],
    apis: ['GET /v1/profiles/{bid_id}', 'POST /v1/consents'],
    related: ['consent-service', 'organization-service', 'credential-service'],
    facets: ['business', 'network', 'lifecycle'], minZoom: 'executive', tone: 'verify',
  }),
  c({
    id: 'candidate-portal', name: 'Candidate Portal', layer: 'experience',
    purpose: 'Where an individual gives informed consent, tracks their own background verification and disputes any finding.',
    responsibilities: ['Present notice before any check runs', 'Capture item-level consent', 'Show the person their own complete result', 'Accept and track disputes', 'Issue consent receipts'],
    inputs: ['Verification invitation', 'Identity proof of the person'],
    outputs: ['Consent artifacts', 'Dispute submissions', 'Consent receipts'],
    dependencies: ['Consent Service', 'Person Service', 'Verification Engine'],
    dataHandled: ['Personal identifiers', 'Education and employment history', 'Check outcomes'],
    security: ['Highest sensitivity class', 'Person sees own data in full', 'Employer sees only what its stated purpose covers', 'Never publicly resolvable'],
    events: ['ConsentGranted', 'DisputeRaised', 'VerificationCompleted'],
    apis: ['POST /v1/consents', 'GET /v1/verification-status'],
    related: ['consent-service', 'person-service', 'privacy-controls'],
    facets: ['people', 'verification', 'security'], minZoom: 'executive', tone: 'verify',
  }),
  c({
    id: 'vendor-portal', name: 'Vendor Portal', layer: 'experience',
    purpose: 'The invited-vendor completion flow — the single highest-leverage surface in the platform, because the network only forms if vendors finish.',
    responsibilities: ['Explain who is asking and why, in plain language', 'Collect only what the policy requires', 'Reuse already-verified attributes', 'Save and resume', 'Mobile-first, multi-language'],
    inputs: ['Secure invitation', 'Requesting policy scope'],
    outputs: ['Submitted attributes', 'Consent for disclosure to the requester'],
    dependencies: ['Campaign Service', 'Consent Service', 'Organization Service'],
    dataHandled: ['Business identifiers', 'Uploaded documents', 'Bank details for ownership proof'],
    security: ['Scoped invitation tokens with expiry', 'Explicit "what the buyer will and will not see" panel', 'Declining is never reported as an adverse signal'],
    events: ['InvitationAccepted', 'VerificationStarted', 'ConsentGranted'],
    apis: ['POST /v1/vendors/invite', 'GET /v1/verification-status'],
    related: ['campaign-service', 'consent-service', 'organization-portal'],
    facets: ['business', 'verification', 'network'], minZoom: 'executive', tone: 'verify',
  }),
  c({
    id: 'public-profile', name: 'BID Public Profile', layer: 'experience',
    purpose: 'The shareable trust surface at bidtrust.in/{bid_id}, strictly separating company-provided information from BID-verified information.',
    responsibilities: ['Render verification status, level, dates and validity', 'Show published credentials', 'Keep claimed and verified data visually distinct', 'Offer the authorized-verification request path'],
    inputs: ['BID ID', 'Publication settings', 'Current verification state'],
    outputs: ['Public profile page', 'Verification request initiation'],
    dependencies: ['Profile Service', 'BID Identity Service', 'Credential Service'],
    dataHandled: ['Public business information only'],
    security: ['No sensitive identifiers exposed', 'No adverse risk findings ever shown publicly', 'Rate-limited and bot-protected', 'Person profiles never publicly resolvable'],
    events: ['ProfileViewed', 'VerificationRequested'],
    apis: ['GET /v1/profiles/{bid_id}'],
    related: ['profile-service', 'bid-identity-service', 'authorization-service'],
    facets: ['network', 'business', 'trust'], minZoom: 'executive', tone: 'verify',
  }),
  c({
    id: 'digital-card', name: 'BID Digital Card', layer: 'experience',
    purpose: 'A signed, shareable card that points at the live profile — a pointer, never a record.',
    responsibilities: ['Render current verification status and level', 'Encode a signed resolution URL in the QR', 'Expire visually when verification lapses'],
    inputs: ['BID ID', 'Verification state'],
    outputs: ['Rendered card', 'Signed QR payload'],
    dependencies: ['Profile Service', 'Credential Service'],
    dataHandled: ['Entity name, BID ID, check-level pass state'],
    security: ['Server-side generation only', 'Signed payload prevents forgery', 'A screenshot proves nothing — only QR resolution does'],
    events: ['CardIssued', 'CardRevoked'],
    apis: ['GET /v1/credentials'],
    related: ['public-profile', 'credential-service'],
    facets: ['network', 'trust'], minZoom: 'executive', tone: 'verify',
  }),
  c({
    id: 'mobile-web', name: 'Mobile / Web Experience', layer: 'experience',
    purpose: 'Responsive delivery across desktop, tablet and mobile — critical because many invited vendors complete verification on a phone.',
    responsibilities: ['Responsive layouts for every portal', 'Offline-tolerant document capture', 'Regional language support', 'Accessible interaction patterns'],
    inputs: ['Device context', 'Locale'],
    outputs: ['Rendered experience'],
    dependencies: ['API Gateway'],
    dataHandled: ['Session state', 'Draft submissions'],
    security: ['No sensitive data cached on device', 'Session timeout', 'Secure document upload'],
    events: [],
    apis: [],
    related: ['vendor-portal', 'candidate-portal'],
    facets: ['business', 'people'], minZoom: 'system', tone: 'neutral',
  }),

  // ───────────────────────────── LAYER 2 — ACCESS ─────────────────────────────
  c({
    id: 'api-gateway', name: 'API Gateway', layer: 'access',
    purpose: 'Single ingress for every client and integration — routing, throttling, validation and observability.',
    responsibilities: ['Request routing and versioning', 'Schema validation', 'Throttling and quota enforcement', 'Request/response logging with PII redaction'],
    inputs: ['HTTP requests', 'API keys and bearer tokens'],
    outputs: ['Routed, validated requests', 'Normalized error responses'],
    dependencies: ['Authentication', 'Rate Limiting', 'Tenant Context'],
    dataHandled: ['Request metadata (payload bodies redacted in logs)'],
    security: ['TLS termination', 'Payload size limits', 'Injection and schema attack surface control', 'Structured audit of privileged calls'],
    events: ['ApiRequestThrottled'],
    apis: ['All /v1/* routes'],
    related: ['authentication', 'rate-limiting', 'api-platform'],
    facets: ['apis', 'security'], minZoom: 'system', tone: 'primary',
  }),
  c({
    id: 'authentication', name: 'Authentication', layer: 'access',
    purpose: 'Establishes who is calling — human user, service account or integration partner.',
    responsibilities: ['Credential and token verification', 'MFA enforcement for privileged roles', 'Session lifecycle', 'Service-account key rotation'],
    inputs: ['Credentials', 'MFA challenge response', 'API keys'],
    outputs: ['Authenticated principal', 'Session token'],
    dependencies: ['SSO', 'Secrets Management'],
    dataHandled: ['Credentials (hashed)', 'Session records', 'Device fingerprints'],
    security: ['MFA mandatory for admin and disclosure-approver roles', 'Brute-force protection', 'No credential logging ever'],
    events: ['UserAuthenticated', 'AuthenticationFailed'],
    apis: ['POST /v1/auth/token'],
    related: ['sso', 'rbac', 'audit-service'],
    facets: ['security', 'apis'], minZoom: 'system', tone: 'primary',
  }),
  c({
    id: 'sso', name: 'SSO', layer: 'access',
    purpose: 'Enterprise single sign-on via SAML/OIDC — a hard procurement requirement above mid-market.',
    responsibilities: ['SAML 2.0 and OIDC federation', 'Just-in-time user provisioning', 'Group-to-role mapping', 'SCIM deprovisioning'],
    inputs: ['IdP assertions', 'Group claims'],
    outputs: ['Federated session', 'Mapped roles'],
    dependencies: ['Authentication', 'RBAC'],
    dataHandled: ['User directory attributes'],
    security: ['Assertion signature validation', 'Replay protection', 'Deprovisioning must revoke access promptly'],
    events: ['UserProvisioned', 'UserDeprovisioned'],
    apis: ['/v1/sso/saml/acs'],
    related: ['authentication', 'rbac', 'access-governance'],
    facets: ['security'], minZoom: 'service', tone: 'primary',
  }),
  c({
    id: 'rbac', name: 'RBAC', layer: 'access',
    purpose: 'Role-based permissions — what a principal may do.',
    responsibilities: ['Role definition and assignment', 'Permission resolution', 'Separation of duties between policy author, reviewer and approver'],
    inputs: ['Authenticated principal', 'Role assignments'],
    outputs: ['Permission set'],
    dependencies: ['Authentication', 'Tenant Context'],
    dataHandled: ['Role and permission assignments'],
    security: ['Least privilege default', 'Assessment-integrity roles kept separate from revenue-owning roles', 'All grants audited'],
    events: ['RoleAssigned', 'RoleRevoked'],
    apis: ['GET /v1/roles'],
    related: ['abac', 'access-governance', 'authentication'],
    facets: ['security'], minZoom: 'system', tone: 'primary',
  }),
  c({
    id: 'abac', name: 'ABAC', layer: 'access',
    purpose: 'Attribute-based rules — whether this principal may act on this specific record, for this purpose.',
    responsibilities: ['Evaluate purpose, consent scope, relationship and sensitivity per request', 'Enforce purpose limitation in code rather than in policy documents', 'Deny by default'],
    inputs: ['Principal attributes', 'Resource attributes', 'Consent scope', 'Stated purpose'],
    outputs: ['Allow/deny decision with reason'],
    dependencies: ['RBAC', 'Consent Service', 'Relationship Service'],
    dataHandled: ['Access decisions and their justifications'],
    security: ['This is where minimum-necessary-disclosure becomes enforceable', 'Every deny is logged with a reason'],
    events: ['AccessDenied'],
    apis: [],
    related: ['rbac', 'consent-service', 'privacy-controls'],
    facets: ['security', 'trust'], minZoom: 'service', tone: 'primary',
  }),
  c({
    id: 'rate-limiting', name: 'Rate Limiting', layer: 'access',
    purpose: 'Protects the platform, upstream providers and per-tenant cost exposure.',
    responsibilities: ['Per-tenant and per-key quotas', 'Burst control', 'Public endpoint bot protection', 'Provider-cost circuit breaking'],
    inputs: ['Request stream', 'Plan entitlements'],
    outputs: ['Throttle decisions', 'Quota headers'],
    dependencies: ['API Gateway', 'Subscription'],
    dataHandled: ['Request counters'],
    security: ['Public profile endpoints hardened against scraping — a harvested verified-business directory has real commercial value'],
    events: ['ApiRequestThrottled', 'QuotaExceeded'],
    apis: [],
    related: ['api-gateway', 'usage', 'public-profile'],
    facets: ['apis', 'security'], minZoom: 'service', tone: 'caution',
  }),
  c({
    id: 'tenant-context', name: 'Tenant Context', layer: 'access',
    purpose: 'Binds every request to exactly one workspace — the mechanism behind the organization-identity vs customer-tenant distinction.',
    responsibilities: ['Resolve workspace from principal', 'Inject tenant context into every query path', 'Reject any query lacking tenant scope'],
    inputs: ['Authenticated principal', 'Workspace membership'],
    outputs: ['Tenant-scoped execution context'],
    dependencies: ['Authentication', 'Organization Service'],
    dataHandled: ['Workspace membership'],
    security: ['Row-level security enforced in the data layer, not the application layer', 'Primary penetration-test target', 'Cross-tenant queries impossible by construction'],
    events: [],
    apis: [],
    related: ['tenant-isolation', 'operational-db', 'relationship-service'],
    facets: ['security', 'data'], minZoom: 'system', tone: 'adverse',
  }),

  // ─────────────────────────── LAYER 3 — CORE PLATFORM ────────────────────────
  c({
    id: 'organization-service', name: 'Organization Service', layer: 'core',
    purpose: 'System of record for the organization — the primary business entity, independent of any commercial role.',
    responsibilities: ['Organization creation, claiming and merging', 'Entity resolution and deduplication', 'Government identifier custody', 'Lifecycle state (member, verified, customer)'],
    inputs: ['Registration data', 'Government identifiers', 'Invitation claims'],
    outputs: ['Canonical organization record', 'BID ID assignment request'],
    dependencies: ['BID Identity Service', 'Operational Database'],
    dataHandled: ['Legal name, registration identifiers, addresses, status'],
    security: ['Deterministic merge only on strong identifier match plus corroboration', 'All merges reversible and attributed', 'PAN and equivalent encrypted at rest'],
    events: ['OrganizationCreated', 'OrganizationMerged', 'OrganizationClaimed'],
    apis: ['POST /v1/organizations', 'GET /v1/organizations/{bid_id}'],
    related: ['bid-identity-service', 'relationship-service', 'profile-service'],
    facets: ['business', 'data', 'network'], minZoom: 'executive', tone: 'primary',
  }),
  c({
    id: 'person-service', name: 'Person Service', layer: 'core',
    purpose: 'System of record for individuals — employees, candidates, contractor workers, directors.',
    responsibilities: ['Person record lifecycle', 'Role state (candidate vs employee) as relationship, not identity', 'Strict consent gating on cross-tenant visibility'],
    inputs: ['Consented identity data', 'Employment relationships'],
    outputs: ['Canonical person record'],
    dependencies: ['Consent Service', 'Operational Database'],
    dataHandled: ['Personal identifiers', 'Contact details', 'Employment links'],
    security: ['Never auto-merged — name collisions are common and the harm is irreversible', 'Never publicly resolvable', 'No searchable database of people, ever'],
    events: ['PersonCreated', 'PersonLinked'],
    apis: ['POST /v1/persons'],
    related: ['consent-service', 'relationship-service', 'privacy-controls'],
    facets: ['people', 'data', 'security'], minZoom: 'executive', tone: 'verify',
  }),
  c({
    id: 'relationship-service', name: 'Relationship Service', layer: 'core',
    purpose: 'Models how entities relate — the unit that carries purpose, and therefore the unit that makes data collection lawful and proportionate.',
    responsibilities: ['Create and maintain typed relationships', 'Attach policy and criticality', 'Track status and review dates', 'Express multi-hop chains (principal → contractor → worker)'],
    inputs: ['Two entities', 'Relationship type', 'Stated purpose', 'Policy reference'],
    outputs: ['Relationship record', 'Verification trigger'],
    dependencies: ['Organization Service', 'Person Service', 'Policy Engine'],
    dataHandled: ['Counterparty pairs', 'Purpose statements', 'Contract references'],
    security: ['Tenant-private — the commercial relationship graph must never leak between tenants', 'A single leak here would be an extinction-level trust event'],
    events: ['RelationshipCreated', 'RelationshipTerminated'],
    apis: ['POST /v1/relationships'],
    related: ['organization-service', 'policy-engine', 'tenant-context'],
    facets: ['business', 'network', 'security'], minZoom: 'executive', tone: 'primary',
  }),
  c({
    id: 'bid-identity-service', name: 'BID Identity Service', layer: 'core',
    purpose: 'Mints and resolves the persistent public handle — BID-BUS, BID-PER, BID-VER, BID-CRED and the rest.',
    responsibilities: ['Random, collision-checked identifier assignment', 'Check-character validation', 'Resolution and permanent redirect after merges', 'Type-scoped visibility rules'],
    inputs: ['Entity type', 'Creation request'],
    outputs: ['BID ID', 'Resolution target'],
    dependencies: ['Operational Database'],
    dataHandled: ['Identifier registry'],
    security: ['No meaning, sequence or PII encoded in the identifier', 'Person IDs never publicly resolvable — a lookup returns the same not-found as a non-existent ID', 'Never reassigned'],
    events: ['BidIdAssigned'],
    apis: ['GET /v1/organizations/{bid_id}'],
    related: ['organization-service', 'public-profile'],
    facets: ['network', 'data'], minZoom: 'system', tone: 'primary',
  }),
  c({
    id: 'profile-service', name: 'Profile Service', layer: 'core',
    purpose: 'Assembles what each audience is allowed to see — public, relationship, authorized or restricted.',
    responsibilities: ['Compose tiered profile views', 'Enforce the claimed-vs-verified separation', 'Apply publication settings', 'Serve the authorized verification view'],
    inputs: ['BID ID', 'Requester identity', 'Consent scope'],
    outputs: ['Tier-appropriate profile payload'],
    dependencies: ['Organization Service', 'Consent Service', 'Assessment Engine'],
    dataHandled: ['Public attributes', 'Verification summaries', 'Consented detail'],
    security: ['Disclosure tier resolved server-side — never trusted from the client', 'Company-provided and BID-verified data structurally separated in the payload itself'],
    events: ['ProfilePublished', 'ProfileViewed'],
    apis: ['GET /v1/profiles/{bid_id}'],
    related: ['public-profile', 'consent-service', 'authorization-service'],
    facets: ['trust', 'network', 'security'], minZoom: 'system', tone: 'verify',
  }),
  c({
    id: 'credential-service', name: 'Credential Service', layer: 'core',
    purpose: 'Manages structured credentials — certificates, licences, registrations — as objects with issuers, validity and revocation, not as files.',
    responsibilities: ['Credential issuance and revocation', 'Validity window tracking', 'Issuer linkage', 'Expiry signalling to the Monitoring Engine'],
    inputs: ['Verified credential data', 'Issuer confirmation'],
    outputs: ['Credential record', 'Expiry events'],
    dependencies: ['Evidence Service', 'Monitoring Engine'],
    dataHandled: ['Credential identifiers, issuers, validity dates'],
    security: ['Files are evidence supporting a credential, never the credential itself — this separation is what makes expiry and revocation possible'],
    events: ['CredentialIssued', 'CredentialExpiring', 'CredentialRevoked'],
    apis: ['GET /v1/credentials'],
    related: ['evidence-service', 'monitoring-engine', 'digital-card'],
    facets: ['trust', 'monitoring'], minZoom: 'system', tone: 'verify',
  }),

  // ──────────────────────────── LAYER 4 — TRUST ENGINE ────────────────────────
  c({
    id: 'policy-engine', name: 'Policy Engine', layer: 'trust',
    purpose: 'Defines what must be verified for a relationship. This is the component that makes BID domain-agnostic.',
    responsibilities: ['Policy creation and authoring', 'Policy versioning (immutable)', 'Required and optional check definition', 'Risk thresholds', 'Expiry and freshness rules', 'Verification frequency', 'Approval and escalation rules', 'Conditional logic evaluation', 'Policy drift reporting'],
    inputs: ['Relationship', 'Entity type', 'Risk level', 'Contract value and category'],
    outputs: ['Verification plan'],
    dependencies: ['Organization Service', 'Verification Engine'],
    dataHandled: ['Policy definitions', 'Version history', 'Threshold configuration'],
    security: ['Policy authoring is a permissioned role', 'Versions are immutable — an auditor asking "what was your standard in March 2026?" must get an unaltered object', 'Changes never silently invalidate completed verifications'],
    events: ['PolicyCreated', 'PolicyUpdated', 'PolicyActivated'],
    apis: ['POST /v1/policies', 'GET /v1/policies/{id}'],
    related: ['verification-engine', 'relationship-service', 'campaign-service'],
    facets: ['trust', 'verification', 'business'], minZoom: 'executive', tone: 'verify',
  }),
  c({
    id: 'verification-engine', name: 'Verification Engine', layer: 'trust',
    purpose: 'Executes the verification plan — decomposes it into checks, routes them, and assembles the result.',
    responsibilities: ['Expand policy into an executable check set', 'Consult the reuse cache before spending money', 'Dispatch to the Provider Orchestrator', 'Track per-check state asynchronously', 'Assemble the verification record'],
    inputs: ['Verification plan', 'Consent scope', 'Cached prior results'],
    outputs: ['Verification record', 'Per-check outcomes', 'Cost ledger entries'],
    dependencies: ['Policy Engine', 'Provider Orchestrator', 'Evidence Service', 'Consent Service'],
    dataHandled: ['Check requests and outcomes', 'Provider correlation ids'],
    security: ['Idempotency keys prevent double-billing', 'Never fabricates a result — unavailable data returns INSUFFICIENT_EVIDENCE with a reason'],
    events: ['VerificationStarted', 'VerificationCompleted', 'VerificationFailed'],
    apis: ['POST /v1/verification-requests', 'GET /v1/verification-requests/{id}'],
    related: ['policy-engine', 'provider-orchestrator', 'assessment-engine'],
    facets: ['verification', 'trust'], minZoom: 'executive', tone: 'verify',
  }),
  c({
    id: 'assessment-engine', name: 'Assessment Engine', layer: 'trust',
    purpose: 'Turns evidence into an explainable assessment — deterministic rubric, never an opaque score.',
    responsibilities: ['Grade each dimension (identity, compliance, financial, documentation, screening, freshness, continuity)', 'Resolve the composite band', 'Produce human-readable justification per dimension', 'Apply freshness decay', 'Enforce hard-stop overrides'],
    inputs: ['Verification results', 'Evidence', 'Policy thresholds'],
    outputs: ['Assessment with per-factor contributions', 'Band', 'Validity window'],
    dependencies: ['Evidence Service', 'Risk Engine', 'Policy Engine'],
    dataHandled: ['Assessment outputs and rubric versions'],
    security: ['Rubric published and versioned', 'INSUFFICIENT EVIDENCE is a distinct state from HIGH RISK — collapsing them would systematically penalise MSMEs', 'No numeric score without published contribution breakdown', 'Assessment never altered for commercial reasons'],
    events: ['AssessmentCompleted', 'AssessmentDisputed'],
    apis: ['GET /v1/assessments/{id}'],
    related: ['risk-engine', 'evidence-service', 'verification-engine'],
    facets: ['trust', 'verification'], minZoom: 'executive', tone: 'verify',
  }),
  c({
    id: 'evidence-service', name: 'Evidence Service', layer: 'trust',
    purpose: 'Immutable, content-addressed custody of everything a verification relied on.',
    responsibilities: ['Write-once evidence capture', 'SHA-256 content addressing', 'Sensitivity classification at capture', 'Retention clock stamped at write time', 'Access-scope enforcement'],
    inputs: ['Provider responses', 'Documents', 'Attestations', 'Field reports'],
    outputs: ['Evidence records with integrity hashes'],
    dependencies: ['Evidence Store', 'Document Storage'],
    dataHandled: ['Raw provider responses', 'Uploaded documents', 'Human verification reports'],
    security: ['Never edited, only appended', 'Tamper-evident hash chain', 'Classified at capture so minimum-necessary-disclosure is enforceable by the system rather than by discipline', 'Legal-hold support'],
    events: ['EvidenceCaptured', 'EvidenceSealed'],
    apis: ['GET /v1/evidence/{id}'],
    related: ['evidence-store', 'audit-service', 'assessment-engine'],
    facets: ['trust', 'data', 'security'], minZoom: 'system', tone: 'verify',
  }),
  c({
    id: 'risk-engine', name: 'Risk Engine', layer: 'trust',
    purpose: 'Screening and adverse-signal detection, with mandatory human disposition.',
    responsibilities: ['Sanctions, PEP and watchlist screening', 'Litigation and adverse media matching', 'False-positive disposition workflow', 'Dismissed-match memory so the same false positive is not re-raised every cycle'],
    inputs: ['Entity and person identifiers', 'Screening lists'],
    outputs: ['Potential matches', 'Dispositioned outcomes'],
    dependencies: ['Provider Orchestrator', 'Evidence Service'],
    dataHandled: ['Screening hits', 'Disposition reasons'],
    security: ['Produces potential matches, never conclusions', 'An undispositioned match never contributes negatively', 'A PEP match is a trigger for enhanced diligence, not an adverse finding'],
    events: ['ScreeningHitRaised', 'ScreeningHitDismissed'],
    apis: ['POST /v1/screenings'],
    related: ['assessment-engine', 'provider-orchestrator'],
    facets: ['trust', 'verification', 'security'], minZoom: 'system', tone: 'caution',
  }),
  c({
    id: 'monitoring-engine', name: 'Monitoring Engine', layer: 'trust',
    purpose: 'Keeps verification alive after onboarding — the capability that converts a transaction into a subscription.',
    responsibilities: ['Watch entity status, GST status, director changes, credential expiry, sanctions and litigation', 'Schedule re-verification per policy', 'Raise alerts to named owners', 'Report policy drift across the vendor base'],
    inputs: ['Monitoring rules', 'Provider change signals', 'Credential validity windows'],
    outputs: ['Monitoring alerts', 're-verification triggers'],
    dependencies: ['Provider Orchestrator', 'Credential Service', 'Notifications'],
    dataHandled: ['Watch rules', 'Change events'],
    security: ['Monitoring stops when consent is revoked', 'Alerts route only to the tenant holding the relationship'],
    events: ['MonitoringAlertCreated', 'ReverificationDue', 'CredentialExpiring'],
    apis: ['POST /v1/monitoring', 'GET /v1/monitoring/alerts'],
    related: ['credential-service', 'notifications', 'verification-engine'],
    facets: ['monitoring', 'trust', 'revenue'], minZoom: 'executive', tone: 'caution',
  }),
  c({
    id: 'document-verification', name: 'Document Verification', layer: 'trust',
    purpose: 'Detects tampering, forgery and anomalies in submitted documents.',
    responsibilities: ['Metadata and layout analysis', 'Template matching against known forgeries', 'Cross-tenant forged-template detection', 'Flag for human review — never auto-conclude'],
    inputs: ['Uploaded documents'],
    outputs: ['Anomaly flags with confidence'],
    dependencies: ['Document Storage', 'Evidence Service'],
    dataHandled: ['Document images and extracted fields'],
    security: ['ML may flag, extract, rank and route — it may never conclude', 'Cross-tenant analysis limited to verification-integrity signals; must never leak commercial relationships'],
    events: ['DocumentAnomalyDetected'],
    apis: ['POST /v1/documents/verify'],
    related: ['evidence-service', 'risk-engine'],
    facets: ['verification', 'trust'], minZoom: 'service', tone: 'caution',
  }),

  // ──────────────────────────── LAYER 5 — GOVERNANCE ──────────────────────────
  c({
    id: 'consent-service', name: 'Consent Service', layer: 'governance',
    purpose: 'Custody of purpose-bound, time-boxed, revocable consent — distinct from authorization.',
    responsibilities: ['Capture specific, informed, item-level consent', 'Issue consent receipts to the grantor', 'Enforce expiry', 'One-click revocation', 'Maintain the grantor-visible register of who holds what'],
    inputs: ['Disclosure request with stated purpose', 'Grantor decision'],
    outputs: ['Consent artifact', 'Consent receipt'],
    dependencies: ['Person Service', 'Organization Service', 'Audit Service'],
    dataHandled: ['Consent scope, purpose, duration, method, artifact hash'],
    security: ['No pre-ticked boxes, no bundling into terms acceptance, no dark patterns', 'Declining is never penalised or reported as adverse', 'Revocation ends future access but cannot erase a buyer’s lawful historical decision record'],
    events: ['ConsentGranted', 'ConsentRevoked', 'ConsentExpired'],
    apis: ['POST /v1/consents', 'DELETE /v1/consents/{id}'],
    related: ['authorization-service', 'privacy-controls', 'abac'],
    facets: ['security', 'trust', 'people'], minZoom: 'executive', tone: 'verify',
  }),
  c({
    id: 'authorization-service', name: 'Authorization Service', layer: 'governance',
    purpose: 'Decides whether a specific requester may see a specific item — separate from consent, which is the subject granting permission.',
    responsibilities: ['Evaluate disclosure tier per request', 'Validate that requested scope is proportionate to stated purpose', 'Challenge over-broad requests', 'Issue time-bound access grants'],
    inputs: ['Requester identity', 'Consent artifacts', 'Stated purpose', 'Relationship context'],
    outputs: ['Authorized view payload scope'],
    dependencies: ['Consent Service', 'ABAC', 'Relationship Service'],
    dataHandled: ['Authorization grants and denials'],
    security: ['Consent and authorization are deliberately separate concepts — consent is the subject’s permission, authorization is the platform’s decision', 'Disproportionate requests are flagged, not silently fulfilled'],
    events: ['AuthorizationGranted', 'AuthorizationDenied'],
    apis: ['POST /v1/authorizations'],
    related: ['consent-service', 'profile-service', 'abac'],
    facets: ['security', 'trust'], minZoom: 'system', tone: 'verify',
  }),
  c({
    id: 'privacy-controls', name: 'Privacy Controls', layer: 'governance',
    purpose: 'Operationalises data-protection obligations — notice, rights, minimization and grievance.',
    responsibilities: ['Notice generation', 'Data principal rights: access, correction, erasure', 'Grievance officer routing with defined turnaround', 'Data minimization enforcement', 'Processing records'],
    inputs: ['Rights requests', 'Processing metadata'],
    outputs: ['Fulfilled rights requests', 'Processing register'],
    dependencies: ['Consent Service', 'Data Retention', 'Audit Service'],
    dataHandled: ['Rights requests', 'Grievance cases'],
    security: ['Built for the stricter interpretation of BID’s dual processor/fiduciary role', 'Individuals under 18 are out of scope entirely — no product need, severe compliance burden'],
    events: ['RightsRequestRaised', 'RightsRequestFulfilled'],
    apis: ['POST /v1/privacy/requests'],
    related: ['consent-service', 'data-retention', 'person-service'],
    facets: ['security', 'people'], minZoom: 'system', tone: 'verify',
  }),
  c({
    id: 'audit-service', name: 'Audit Service', layer: 'governance',
    purpose: 'Append-only record of everything that happened — the basis of the audit-grade claim.',
    responsibilities: ['Log every sensitive-data read with actor, purpose and timestamp', 'Hash-chain entries for tamper evidence', 'Serve auditor-facing exports', 'Retain per retention policy'],
    inputs: ['Events from every service'],
    outputs: ['Immutable audit entries', 'Audit exports'],
    dependencies: ['Audit Store', 'Event Store'],
    dataHandled: ['Actor, action, resource, purpose, timestamp'],
    security: ['Append-only — no service may edit or delete', 'Hash chaining makes tampering detectable', 'An audit trail that begins in month twelve is not an audit trail'],
    events: ['AuditEntryWritten'],
    apis: ['GET /v1/audit/export'],
    related: ['audit-store', 'evidence-service', 'access-governance'],
    facets: ['security', 'data'], minZoom: 'executive', tone: 'primary',
  }),
  c({
    id: 'data-retention', name: 'Data Retention', layer: 'governance',
    purpose: 'Enforces storage limitation — retention is a field stamped at write time, not a promise in a policy document.',
    responsibilities: ['Apply per-category retention clocks', 'Run automated deletion', 'Honour legal hold', 'Prove deletion across replicas, backups, caches and search indexes'],
    inputs: ['Retention configuration', 'Legal holds'],
    outputs: ['Deletion jobs and certificates'],
    dependencies: ['Operational Database', 'Evidence Store', 'Document Storage'],
    dataHandled: ['Retention schedules'],
    security: ['Deletion that leaves data in a search index is a lie — the most common way this fails in practice', 'Legal hold overrides deletion and is itself recorded'],
    events: ['RetentionApplied', 'LegalHoldPlaced'],
    apis: [],
    related: ['privacy-controls', 'evidence-store', 'audit-service'],
    facets: ['security', 'data'], minZoom: 'service', tone: 'primary',
  }),
  c({
    id: 'access-governance', name: 'Access Governance', layer: 'governance',
    purpose: 'Periodic review of who can reach what, including BID’s own staff.',
    responsibilities: ['Access recertification campaigns', 'Privileged-access review', 'Production access approval, time-boxing and session recording', 'Segregation-of-duties validation'],
    inputs: ['Role assignments', 'Access logs'],
    outputs: ['Recertification outcomes', 'Revocations'],
    dependencies: ['RBAC', 'Audit Service'],
    dataHandled: ['Access review records'],
    security: ['Customer data never used in non-production environments', 'Production access approved, time-bound, recorded and reviewed'],
    events: ['AccessReviewCompleted', 'AccessRevoked'],
    apis: [],
    related: ['rbac', 'audit-service', 'sso'],
    facets: ['security'], minZoom: 'service', tone: 'primary',
  }),

  // ───────────────────────────── LAYER 6 — BUSINESS ───────────────────────────
  c({
    id: 'campaign-service', name: 'Campaign Service', layer: 'business',
    purpose: 'Runs bulk verification programmes — the vendor onboarding wedge in product form.',
    responsibilities: ['Create campaigns against a policy', 'Invite entities in bulk', 'Track invited/registered/completed/pending/exception', 'Chase non-responders', 'Report completion rate per check'],
    inputs: ['Entity list', 'Policy version', 'Requesting tenant'],
    outputs: ['Invitations', 'Campaign progress', 'Exception queue'],
    dependencies: ['Policy Engine', 'Verification Engine', 'Notifications'],
    dataHandled: ['Campaign membership', 'Invitation state'],
    security: ['Campaign membership is tenant-private', 'Invitation tokens scoped and expiring'],
    events: ['CampaignCreated', 'InvitationSent', 'InvitationAccepted'],
    apis: ['POST /v1/campaigns', 'POST /v1/vendors/invite'],
    related: ['policy-engine', 'vendor-portal', 'notifications'],
    facets: ['business', 'verification', 'network'], minZoom: 'executive', tone: 'primary',
  }),
  c({
    id: 'customer-lifecycle', name: 'Customer Lifecycle', layer: 'business',
    purpose: 'Tracks an organization’s journey from unknown, through free member, to paying requester and beyond.',
    responsibilities: ['Maintain lifecycle stage', 'Detect activation and expansion signals', 'Drive stage-appropriate product prompts', 'Feed Customer Success health scoring'],
    inputs: ['Product usage', 'Verification activity', 'Billing state'],
    outputs: ['Lifecycle stage transitions'],
    dependencies: ['Usage', 'Subscription', 'Customer Success'],
    dataHandled: ['Stage history', 'Activation timestamps'],
    security: ['BID Member and BID Customer are distinct states — being verified never implies a commercial relationship'],
    events: ['MemberActivated', 'RequesterActivated', 'CustomerConverted', 'CustomerAtRisk', 'CustomerChurned'],
    apis: ['GET /v1/customers/{id}/lifecycle'],
    related: ['customer-success', 'subscription', 'organization-service'],
    facets: ['lifecycle', 'business', 'revenue'], minZoom: 'executive', tone: 'primary',
  }),
  c({
    id: 'billing', name: 'Billing', layer: 'business',
    purpose: 'Invoicing, payment capture and reconciliation.',
    responsibilities: ['Generate invoices from subscription and usage', 'Capture payments', 'Dunning', 'Reconcile provider costs against revenue'],
    inputs: ['Subscription terms', 'Usage records', 'Credit consumption'],
    outputs: ['Invoices', 'Payment records'],
    dependencies: ['Subscription', 'Usage', 'Credits'],
    dataHandled: ['Invoices, payments, tax data'],
    security: ['No card data stored — delegated to the payment processor', 'Financial records retained per statutory requirement'],
    events: ['InvoiceIssued', 'PaymentReceived', 'PaymentFailed'],
    apis: ['GET /v1/invoices'],
    related: ['subscription', 'usage', 'credits'],
    facets: ['revenue', 'business'], minZoom: 'system', tone: 'primary',
  }),
  c({
    id: 'subscription', name: 'Subscription', layer: 'business',
    purpose: 'Plan entitlements and term management.',
    responsibilities: ['Plan assignment and upgrades', 'Entitlement resolution (users, policies, monitored entities, API access)', 'Term and renewal tracking'],
    inputs: ['Plan selection', 'Contract terms'],
    outputs: ['Active entitlements'],
    dependencies: ['Billing', 'Customer Lifecycle'],
    dataHandled: ['Plan, term, entitlement configuration'],
    security: ['Entitlement checks enforced server-side at every feature boundary'],
    events: ['SubscriptionCreated', 'SubscriptionUpgraded', 'SubscriptionCancelled'],
    apis: ['GET /v1/subscriptions'],
    related: ['billing', 'usage', 'rate-limiting'],
    facets: ['revenue', 'business'], minZoom: 'system', tone: 'primary',
  }),
  c({
    id: 'usage', name: 'Usage', layer: 'business',
    purpose: 'Meters consumption — verifications, monitored entities, API calls.',
    responsibilities: ['Record every billable event', 'Aggregate per tenant and period', 'Feed overage billing', 'Surface utilisation to Customer Success'],
    inputs: ['Verification completions', 'API calls', 'Monitoring subscriptions'],
    outputs: ['Usage aggregates'],
    dependencies: ['Verification Engine', 'API Platform'],
    dataHandled: ['Metered counters'],
    security: ['Usage data is tenant-scoped and never aggregated across tenants in a way that reveals activity levels'],
    events: ['UsageRecorded', 'QuotaExceeded'],
    apis: ['GET /v1/usage'],
    related: ['billing', 'credits', 'customer-success'],
    facets: ['revenue', 'business'], minZoom: 'service', tone: 'primary',
  }),
  c({
    id: 'credits', name: 'Credits', layer: 'business',
    purpose: 'Prepaid verification balances with expiry.',
    responsibilities: ['Credit purchase and top-up', 'Draw-down against verifications', 'Expiry and breakage handling', 'Low-balance alerting'],
    inputs: ['Credit purchases', 'Verification costs'],
    outputs: ['Balance state', 'Breakage records'],
    dependencies: ['Billing', 'Usage'],
    dataHandled: ['Credit ledgers'],
    security: ['Ledger is append-only; balance is derived, never directly mutated'],
    events: ['CreditsPurchased', 'CreditsConsumed', 'CreditsExpired'],
    apis: ['GET /v1/credits'],
    related: ['billing', 'usage'],
    facets: ['revenue'], minZoom: 'service', tone: 'primary',
  }),
  c({
    id: 'notifications', name: 'Notifications', layer: 'business',
    purpose: 'Multi-channel delivery — invitations, chasers, alerts, approvals.',
    responsibilities: ['Email, SMS and WhatsApp delivery', 'Template and language management', 'Chase sequences for incomplete verifications', 'Delivery and bounce tracking'],
    inputs: ['Domain events', 'Recipient preferences'],
    outputs: ['Delivered messages', 'Delivery receipts'],
    dependencies: ['Event Store', 'Campaign Service'],
    dataHandled: ['Contact details', 'Message content'],
    security: ['No sensitive verification detail in message bodies — messages link to authenticated surfaces', 'Unsubscribe honoured except for statutory notices'],
    events: ['NotificationSent', 'NotificationBounced'],
    apis: ['POST /v1/notifications'],
    related: ['campaign-service', 'monitoring-engine'],
    facets: ['business', 'monitoring'], minZoom: 'service', tone: 'neutral',
  }),
  c({
    id: 'customer-success', name: 'Customer Success', layer: 'business',
    purpose: 'Health scoring and intervention — the operational half of the customer lifecycle.',
    responsibilities: ['Compute health from login, verification volume, policy usage, API usage, monitoring, credit utilisation, tickets and payment status', 'Flag at-risk accounts', 'Drive playbooks', 'Track win-back'],
    inputs: ['Usage signals', 'Support cases', 'Billing state'],
    outputs: ['Health score', 'Risk flags', 'Playbook triggers'],
    dependencies: ['Usage', 'Customer Lifecycle', 'Billing'],
    dataHandled: ['Health signals and scores'],
    security: ['Health scoring uses tenant-internal signals only'],
    events: ['CustomerAtRisk', 'CustomerRetained', 'CustomerChurned'],
    apis: ['GET /v1/customers/health'],
    related: ['customer-lifecycle', 'usage'],
    facets: ['lifecycle', 'business', 'revenue'], minZoom: 'system', tone: 'caution',
  }),

  // ─────────────────────────── LAYER 7 — INTEGRATION ──────────────────────────
  c({
    id: 'provider-orchestrator', name: 'Provider Orchestrator', layer: 'integration',
    purpose: 'Routes each check to a qualified provider and normalizes what comes back — the layer that prevents vendor lock-in.',
    responsibilities: ['Capability, eligibility, confidence, health and cost routing', 'Fallback chains and circuit breaking', 'Response normalization to a canonical schema', 'Cost ledger per call', 'SLA and quality monitoring', 'Reuse-cache consultation'],
    inputs: ['Check request', 'Required verification level', 'Consent scope'],
    outputs: ['Normalized check result', 'Cost record', 'Evidence payload'],
    dependencies: ['External provider adapters', 'Evidence Service'],
    dataHandled: ['Provider requests and responses', 'Cost data'],
    security: ['Never retries a definitive negative — that is a billed way to get the same answer', 'Honours each provider’s permitted-use, retention and caching terms', 'At least two qualified routes for every REQUIRED check'],
    events: ['ProviderCallCompleted', 'ProviderFailover', 'ProviderCircuitOpened'],
    apis: [],
    related: ['verification-engine', 'evidence-service', 'provider-kyb'],
    facets: ['providers', 'verification'], minZoom: 'executive', tone: 'primary',
  }),
  c({
    id: 'api-platform', name: 'API Platform', layer: 'integration',
    purpose: 'The public, versioned API surface for customers and platform partners.',
    responsibilities: ['Versioned REST endpoints', 'API key and scope management', 'Developer sandbox', 'SDKs and documentation'],
    inputs: ['Partner and customer API calls'],
    outputs: ['API responses', 'Usage records'],
    dependencies: ['API Gateway', 'Usage'],
    dataHandled: ['Everything the caller is entitled to, and nothing more'],
    security: ['Scoped keys, rotation, per-key quotas', 'Sandbox uses synthetic data only'],
    events: ['ApiKeyIssued', 'ApiKeyRevoked'],
    apis: ['All /v1/* routes'],
    related: ['api-gateway', 'webhooks', 'usage'],
    facets: ['apis'], minZoom: 'system', tone: 'primary',
  }),
  c({
    id: 'webhooks', name: 'Webhooks', layer: 'integration',
    purpose: 'Pushes domain events to customer systems.',
    responsibilities: ['Subscription management', 'Signed delivery', 'Retry with backoff', 'Replay of missed events'],
    inputs: ['Domain events', 'Endpoint subscriptions'],
    outputs: ['Signed HTTP callbacks'],
    dependencies: ['Event Store'],
    dataHandled: ['Event payloads (minimal — identifiers, not detail)'],
    security: ['HMAC-signed payloads', 'Payloads carry identifiers, not sensitive detail — the consumer fetches detail over an authenticated call'],
    events: ['WebhookDelivered', 'WebhookFailed'],
    apis: ['POST /v1/webhooks'],
    related: ['api-platform', 'event-store'],
    facets: ['apis'], minZoom: 'service', tone: 'primary',
  }),
  c({
    id: 'enterprise-integrations', name: 'Enterprise Integrations', layer: 'integration',
    purpose: 'Connector framework for customer systems of record.',
    responsibilities: ['Field mapping and transformation', 'Sync scheduling and conflict handling', 'Connector health monitoring'],
    inputs: ['Customer system records'],
    outputs: ['Synchronised entity and verification state'],
    dependencies: ['API Platform'],
    dataHandled: ['Vendor master data', 'Employee records'],
    security: ['Least-privilege service accounts on both sides', 'Field-level mapping avoids over-collection'],
    events: ['IntegrationSyncCompleted', 'IntegrationSyncFailed'],
    apis: [],
    related: ['erp-connector', 'hrms-connector', 'api-platform'],
    facets: ['apis', 'business'], minZoom: 'system', tone: 'primary',
  }),
  c({
    id: 'hrms-connector', name: 'HRMS', layer: 'integration',
    purpose: 'Connector to human resource systems for employee and candidate verification.',
    responsibilities: ['Sync candidate and employee records', 'Trigger BGV on hiring events', 'Write verification status back'],
    inputs: ['Candidate and employee records'],
    outputs: ['BGV requests', 'Status writeback'],
    dependencies: ['Enterprise Integrations', 'Verification Engine'],
    dataHandled: ['Personal data — highest sensitivity'],
    security: ['Consent must exist before any check is triggered by a sync', 'Minimal field mapping'],
    events: ['BgvRequested'],
    apis: ['POST /v1/bgv/requests'],
    related: ['enterprise-integrations', 'person-service'],
    facets: ['people', 'apis'], minZoom: 'service', tone: 'external',
  }),
  c({
    id: 'erp-connector', name: 'ERP', layer: 'integration',
    purpose: 'Connector to enterprise resource planning systems for vendor master data.',
    responsibilities: ['Sync vendor master records', 'Push BID ID into the vendor master', 'Block payment setup on unverified counterparties where the customer configures it'],
    inputs: ['Vendor master records'],
    outputs: ['Verification status', 'BID ID writeback'],
    dependencies: ['Enterprise Integrations'],
    dataHandled: ['Vendor master data', 'Bank details'],
    security: ['Bank account ownership verification is a direct payment-fraud control at this boundary'],
    events: ['VendorMasterSynced'],
    apis: [],
    related: ['enterprise-integrations', 'organization-service'],
    facets: ['business', 'apis'], minZoom: 'service', tone: 'external',
  }),
  c({
    id: 'procurement-connector', name: 'Procurement', layer: 'integration',
    purpose: 'Connector to procurement and sourcing suites.',
    responsibilities: ['Embed verification status in onboarding workflows', 'Gate supplier approval on policy outcome', 'Surface monitoring alerts in the suite'],
    inputs: ['Supplier onboarding requests'],
    outputs: ['Verification outcomes into the suite'],
    dependencies: ['Enterprise Integrations', 'Campaign Service'],
    dataHandled: ['Supplier records'],
    security: ['Integrate rather than compete — the suite owns the commercial relationship, BID owns the verification'],
    events: ['SupplierOnboardingGated'],
    apis: [],
    related: ['enterprise-integrations', 'campaign-service'],
    facets: ['business', 'apis'], minZoom: 'service', tone: 'external',
  }),
  c({
    id: 'ats-connector', name: 'ATS', layer: 'integration',
    purpose: 'Connector to applicant tracking systems.',
    responsibilities: ['Trigger candidate BGV at offer stage', 'Return status to the recruiter view', 'Preserve candidate consent flow'],
    inputs: ['Candidate records at a defined stage'],
    outputs: ['BGV status'],
    dependencies: ['Enterprise Integrations', 'Consent Service'],
    dataHandled: ['Candidate personal data'],
    security: ['Notice and consent precede every check regardless of what the ATS triggers'],
    events: ['BgvRequested'],
    apis: ['POST /v1/bgv/requests'],
    related: ['hrms-connector', 'candidate-portal'],
    facets: ['people', 'apis'], minZoom: 'service', tone: 'external',
  }),
  c({
    id: 'crm-connector', name: 'CRM', layer: 'integration',
    purpose: 'Connector to CRM for counterparty diligence and lifecycle signals.',
    responsibilities: ['Verify business partners and channel entities', 'Surface lifecycle stage to account teams'],
    inputs: ['Account records'],
    outputs: ['Verification status', 'Lifecycle signals'],
    dependencies: ['Enterprise Integrations'],
    dataHandled: ['Account and contact records'],
    security: ['No BID relationship-graph data flows outward'],
    events: [],
    apis: [],
    related: ['enterprise-integrations', 'customer-lifecycle'],
    facets: ['business', 'apis'], minZoom: 'infrastructure', tone: 'external',
  }),

  // ─────────────────────── LAYER 8 — EXTERNAL PROVIDERS ───────────────────────
  ...([
    ['provider-kyb', 'KYB', 'Business identity and registry verification.', 'Corporate identity, registration status, entity attributes'],
    ['provider-identity', 'Identity', 'Individual identity verification through compliant, consent-based routes.', 'Identity confirmation'],
    ['provider-gst', 'GST', 'Tax registration status and attributes via authorized channels.', 'Registration status, legal name, place of business'],
    ['provider-mca', 'MCA', 'Corporate registry data — directors, filings, status.', 'Directors, incorporation, entity status'],
    ['provider-bank', 'Bank Verification', 'Account ownership proof via banking rails.', 'Account ownership and name match'],
    ['provider-education', 'Education', 'Institution and qualification confirmation.', 'Degree and institution confirmation'],
    ['provider-employment', 'Employment', 'Previous employer and statutory employment record confirmation.', 'Employment dates and designation'],
    ['provider-address', 'Address', 'Digital and physical address verification.', 'Address confirmation'],
    ['provider-legal', 'Legal / Risk', 'Court record and litigation matching.', 'Potential court-record matches'],
    ['provider-aml', 'AML / Sanctions', 'Sanctions, PEP and watchlist screening.', 'Potential screening matches'],
    ['provider-docs', 'Document Verification', 'Third-party document authenticity analysis.', 'Document anomaly signals'],
    ['provider-credentials', 'Digital Credentials', 'Issuer-attested digital credential verification.', 'Issuer confirmation'],
  ] as const).map(([id, name, purpose, output]) =>
    c({
      id, name, layer: 'providers', purpose,
      responsibilities: ['Execute the requested check', 'Return a provider-native response', 'Honour contracted SLA'],
      inputs: ['Check request with identifiers', 'Consent reference where required'],
      outputs: [output],
      dependencies: ['Provider Orchestrator'],
      dataHandled: ['Only the identifiers necessary for the check'],
      security: [
        'Mock adapter in this prototype — no third-party API is actually integrated',
        'Access eligibility varies per channel and may be legally restricted; product must not assume access',
        'Caching and reuse rights must be secured contractually or the network economics do not exist',
      ],
      events: ['ProviderCallCompleted', 'ProviderFailover'],
      apis: [],
      related: ['provider-orchestrator'],
      facets: ['providers', 'verification'], minZoom: 'system', tone: 'external',
    }),
  ),

  // ─────────────────────────────── LAYER 9 — DATA ─────────────────────────────
  c({
    id: 'operational-db', name: 'Operational Database', layer: 'data',
    purpose: 'Transactional store for entities, relationships, policies and verification state.',
    responsibilities: ['ACID transactions', 'Row-level tenant security', 'Referential integrity', 'Point-in-time recovery'],
    inputs: ['Service writes'],
    outputs: ['Query results'],
    dependencies: [],
    dataHandled: ['Organizations, persons, relationships, policies, verifications'],
    security: ['Row-level security with tenant context in every query path', 'Field-level encryption for sensitive identifiers', 'Separate key custody'],
    events: [],
    apis: [],
    related: ['tenant-context', 'evidence-store'],
    facets: ['data', 'security'], minZoom: 'system', tone: 'primary',
  }),
  c({
    id: 'evidence-store', name: 'Evidence Store', layer: 'data',
    purpose: 'Write-once, content-addressed custody of verification evidence.',
    responsibilities: ['Immutable object storage', 'Hash verification on read', 'Retention clock enforcement', 'Legal hold'],
    inputs: ['Evidence objects'],
    outputs: ['Verified-integrity evidence'],
    dependencies: [],
    dataHandled: ['Provider responses, documents, attestations'],
    security: ['Write-once', 'Independent auditor verifiability', 'HIGHLY SENSITIVE partition kept separate with independent keys'],
    events: [],
    apis: [],
    related: ['evidence-service', 'document-storage'],
    facets: ['data', 'trust', 'security'], minZoom: 'system', tone: 'verify',
  }),
  c({
    id: 'document-storage', name: 'Document Storage', layer: 'data',
    purpose: 'Encrypted object storage for submitted documents.',
    responsibilities: ['Encrypted upload and retrieval', 'Virus and malware scanning', 'Signed, expiring access URLs'],
    inputs: ['Uploaded files'],
    outputs: ['Scanned, stored objects'],
    dependencies: [],
    dataHandled: ['Certificates, licences, statements, identity documents'],
    security: ['Encryption at rest with per-tenant key scoping', 'Time-limited signed URLs only — never public objects'],
    events: [],
    apis: [],
    related: ['evidence-store', 'document-verification'],
    facets: ['data', 'security'], minZoom: 'service', tone: 'primary',
  }),
  c({
    id: 'audit-store', name: 'Audit Store', layer: 'data',
    purpose: 'Append-only, hash-chained audit persistence.',
    responsibilities: ['Append-only writes', 'Chain integrity verification', 'Long-retention storage', 'Auditor export'],
    inputs: ['Audit entries'],
    outputs: ['Verifiable audit history'],
    dependencies: [],
    dataHandled: ['Actor, action, resource, purpose, timestamp'],
    security: ['No update or delete path exists for any service', 'Chain breaks are alertable events'],
    events: [],
    apis: [],
    related: ['audit-service'],
    facets: ['data', 'security'], minZoom: 'system', tone: 'primary',
  }),
  c({
    id: 'event-store', name: 'Event Store', layer: 'data',
    purpose: 'Durable domain-event log and the backbone of the event-driven architecture.',
    responsibilities: ['Ordered, durable event persistence', 'Consumer offset tracking', 'Replay for new consumers and recovery'],
    inputs: ['Domain events from every service'],
    outputs: ['Event streams to consumers'],
    dependencies: [],
    dataHandled: ['Event envelopes with minimal payloads'],
    security: ['Payloads carry identifiers, not sensitive detail — consumers fetch detail over authorized calls'],
    events: [],
    apis: [],
    related: ['webhooks', 'audit-service', 'analytics'],
    facets: ['data', 'apis'], minZoom: 'system', tone: 'primary',
  }),
  c({
    id: 'analytics', name: 'Analytics', layer: 'data',
    purpose: 'Product and operational analytics within strict privacy boundaries.',
    responsibilities: ['Funnel and completion-rate analysis', 'Cost-per-verification tracking', 'Provider quality comparison', 'Policy pass-rate reporting'],
    inputs: ['Events', 'Usage aggregates'],
    outputs: ['Dashboards and metrics'],
    dependencies: ['Event Store', 'Data Warehouse'],
    dataHandled: ['Aggregated, de-identified metrics'],
    security: ['HIGHLY SENSITIVE data never enters analytics', 'No feature may allow inference of the cross-tenant relationship graph'],
    events: [],
    apis: [],
    related: ['data-warehouse', 'customer-success'],
    facets: ['data', 'business'], minZoom: 'service', tone: 'neutral',
  }),
  c({
    id: 'data-warehouse', name: 'Data Warehouse', layer: 'data',
    purpose: 'Analytical store for aggregate reporting and benchmark research.',
    responsibilities: ['Dimensional modelling', 'Aggregation pipelines', 'Anonymised benchmark datasets'],
    inputs: ['Operational extracts', 'Event streams'],
    outputs: ['Analytical datasets'],
    dependencies: ['Event Store'],
    dataHandled: ['Aggregated, anonymised data'],
    security: ['Aggregate benchmark outputs must not be re-identifiable to a tenant or counterparty'],
    events: [],
    apis: [],
    related: ['analytics'],
    facets: ['data'], minZoom: 'infrastructure', tone: 'neutral',
  }),
]

export const LAYERS: ArchLayer[] = [
  { id: 'experience', index: 1, name: 'Experience', caption: 'Portals, profile and card surfaces', tone: 'verify', componentIds: ['enterprise-portal', 'organization-portal', 'candidate-portal', 'vendor-portal', 'public-profile', 'digital-card', 'mobile-web'] },
  { id: 'access', index: 2, name: 'Access', caption: 'Gateway, identity and tenant boundary', tone: 'primary', componentIds: ['api-gateway', 'authentication', 'sso', 'rbac', 'abac', 'rate-limiting', 'tenant-context'] },
  { id: 'core', index: 3, name: 'Core Platform', caption: 'Entities, relationships and identity', tone: 'primary', componentIds: ['organization-service', 'person-service', 'relationship-service', 'bid-identity-service', 'profile-service', 'credential-service'] },
  { id: 'trust', index: 4, name: 'Trust Engine', caption: 'Policy, verification, evidence and assessment', tone: 'verify', componentIds: ['policy-engine', 'verification-engine', 'assessment-engine', 'evidence-service', 'risk-engine', 'monitoring-engine', 'document-verification'] },
  { id: 'governance', index: 5, name: 'Governance', caption: 'Consent, authorization, privacy and audit', tone: 'verify', componentIds: ['consent-service', 'authorization-service', 'privacy-controls', 'audit-service', 'data-retention', 'access-governance'] },
  { id: 'business', index: 6, name: 'Business', caption: 'Campaigns, lifecycle, billing and success', tone: 'primary', componentIds: ['campaign-service', 'customer-lifecycle', 'billing', 'subscription', 'usage', 'credits', 'notifications', 'customer-success'] },
  { id: 'integration', index: 7, name: 'Integration', caption: 'Orchestration, API platform and connectors', tone: 'primary', componentIds: ['provider-orchestrator', 'api-platform', 'webhooks', 'enterprise-integrations', 'hrms-connector', 'erp-connector', 'procurement-connector', 'ats-connector', 'crm-connector'] },
  { id: 'providers', index: 8, name: 'External Providers', caption: 'Mock adapters — no live third-party integration', tone: 'external', componentIds: ['provider-kyb', 'provider-identity', 'provider-gst', 'provider-mca', 'provider-bank', 'provider-education', 'provider-employment', 'provider-address', 'provider-legal', 'provider-aml', 'provider-docs', 'provider-credentials'] },
  { id: 'data', index: 9, name: 'Data', caption: 'Operational, evidence, audit and analytical stores', tone: 'primary', componentIds: ['operational-db', 'evidence-store', 'document-storage', 'audit-store', 'event-store', 'analytics', 'data-warehouse'] },
]

export const COMPONENT_BY_ID = new Map(COMPONENTS.map((x) => [x.id, x]))
export const LAYER_BY_ID = new Map(LAYERS.map((l) => [l.id, l]))
