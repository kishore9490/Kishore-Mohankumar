# Local SQLite file. Nothing to install — created on first `npm run db:push`.
DATABASE_URL="file:./bid.db"

# Signs session cookies. Generate your own before deploying anywhere real:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET="dev-only-secret-change-me-before-deploying"
